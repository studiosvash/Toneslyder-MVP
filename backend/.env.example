# Example environment variables for ToneSlyder backend

# Port for the Express server
PORT=8787

# Provider settings: "mock" or "real"
PROVIDER_MODE=mock

# OpenAI provider configuration (used when PROVIDER_MODE=real)
API_KEY=
MODEL_ID=gpt-4o-mini
API_BASE=https://api.openai.com/v1

# HTTPS configuration
HTTPS_ENABLE=false
HTTPS_CERT_PATH=./certs/localhost.pem
HTTPS_KEY_PATH=./certs/localhost-key.pem
